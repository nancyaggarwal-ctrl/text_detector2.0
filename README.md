This is a simple C++ command-line program that reads a text file and searches for a specific word entered by the user. It displays:

The line numbers where the word is found.

The total number of occurrences in the entire file.

This can be useful for basic text analysis, searching logs, and learning file handling in C++.

🚀 Features:

Reads any text file from a given path

Converts input to lowercase for case-insensitive matching

Displays all line numbers where the word is found

Shows the total count at the end

🛠 Technologies Used:

C++;

<fstream> for file handling

<algorithm> for lowercase conversion

<sstream> for splitting lines into words

✍ Future Improvements:

Support search for entire phrases

Highlight matching words when printing lines

Add GUI version

Allow multiple file search
